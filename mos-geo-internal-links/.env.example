# Copy to a .env OUTSIDE this repo (e.g. ~/.config/mos/typesafe.env) and pass it with --env-file.
# Create a key at https://console.typesafe.ai/keys. The scripts never print it.
TYPESAFE_API_KEY=
# Optional: TYPESAFE_BASE_URL=https://api.typesafe.ai
